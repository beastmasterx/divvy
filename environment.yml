name: divvy
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - sqlalchemy>=2.0.0
  - python-dotenv>=1.0.0
  - fastapi>=0.121.0
  - uvicorn>=0.38.0
  - pydantic>=2.0.0
  - email-validator>=2.3.0
  - python-multipart>=0.0.20
  - pip
  - pip:
    - -e ".[dev]"  # Installs dev tools from pyproject.toml
